// -- IMPORTS

import "package:flutter/material.dart";
import "start_page.dart";

// -- TYPES

class START_STATE extends State<START_PAGE>
{
    // -- CONSTRUCTORS

    START_STATE(
        ) :
        super()
    {
    }

    // -- OPERATIONS

    Widget build(
        BuildContext build_context
        )
    {
        return Scaffold(
            body: SizedBox()
            );
    }
}
